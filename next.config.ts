import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Server-rendered mode for API routes (Vercel compatible)
  // Videos and static assets served from /public
};

export default nextConfig;
